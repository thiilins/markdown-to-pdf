import { GlobalToolLayout } from '@/shared/layouts/tools/global-tool'

export default function ToolsLayout({ children }: { children: React.ReactNode }) {
  return <GlobalToolLayout>{children}</GlobalToolLayout>
}
